using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Fuka {
    public class guloseimasSpawner : MonoBehaviour
    {
        // Start is called before the first frame update
        public GameObject[] spots;
        private Fuka.GameManager fukaGm;
        void Start()
        {
            // fukaGm = Fuka.GameManager.GetInstance();
            // for
        }

        // Update is called once per frame
        void Update()
        {
            
        }
    }
}